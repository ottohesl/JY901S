# JY901S
基于stm32HAL库的任意芯片（支持h7系列）jy901s代码，主程序调用两行代码就可以随时随地获取陀螺仪任意数据。基于stm32HAL库任意芯片的jy901s代码，可在stm32cubemx上配置两个串口就可用，可直接调用函数于主程序或是其他文件使用。写有常用修改输出速率的命令、软件修改波特率等功能；可直接获取加速度，角速度与欧拉角，同样包含磁场、四元数和芯片温度读取功能。
项目只需要将当前串口（与jy901s连接的）的配置打开dma并设置循环模式就可以。
